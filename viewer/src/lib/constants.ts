/** Debounce delay for search/typeahead inputs (ms). */
export const SEARCH_DEBOUNCE_MS = 300;

/** Debounce delay for setup-assistant field changes before persisting (ms). */
export const SETUP_ASSISTANT_DEBOUNCE_MS = 300;
