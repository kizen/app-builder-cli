// Runs from the settings dropdown on an object's record-list page. The script
// gets an object context only: `this.objectId` is set and `this.entityId` is
// empty.
//
// Keep this file non-empty. Publishing an app requires a non-blank script for
// object-settings items.

this.showToast('Hello from an object settings item.', { variant: 'success' });
